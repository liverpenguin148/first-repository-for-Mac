<% unless tasks.nil? %>
<% tasks.each do |task| %>
<div class="card border-primary">
  <div class="taskname">
    <%= task.content %>
  </div>
  <%= form_with(model: task, remote: true) do |f| %>
  <div><%= hidden_field_tag :status, task.finished %></div>
  <%= f.submit '着手', class: 'btn btn-outline-primary' %>
  <% end %>
</div>
<% end %>
<% end %>